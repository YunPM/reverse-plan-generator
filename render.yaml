services:
  - type: web
    name: reverse-plan-generator
    runtime: docker
    plan: free
    envVars:
      - key: GEMINI_API_KEY
        sync: false   # Render 대시보드에서 직접 입력
    healthCheckPath: /api/health
