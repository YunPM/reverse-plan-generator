# 배포 기록 — 2026-03-16

## Hotfix: Dockerfile에서 playwright 설치 명령 제거

PR: hotfix/remove-playwright-dockerfile → main (PR URL: GitHub에서 생성 필요)

### 문제 원인

- `requirements.txt`에 `playwright` 패키지가 없는 상태에서 Dockerfile이 `playwright install chromium` 명령을 실행
- Docker 빌드 시 `playwright: not found` 에러 발생으로 배포 실패
- 백엔드에서 playwright를 실제로 사용하지 않으므로 해당 명령이 불필요

### 변경 파일

- `Dockerfile` (36~37번째 줄 2줄 삭제)
  - `# Playwright Chromium 설치` 주석
  - `RUN playwright install chromium && playwright install-deps chromium`

### 자동 검증 결과

- ⬜ Docker 미실행으로 자동 검증 미수행 (서버 미실행 상태)
  - pytest -v
  - 타겟 API 검증 (변경분 없으므로 해당 없음)
  - Playwright 타겟 검증 (변경분 없으므로 해당 없음)

### 코드 리뷰 결과

- Critical 이슈: 없음
- High 이슈: 없음
- Medium/Low 이슈: 없음
- 순수 삭제 패치 (불필요한 명령 2줄 제거), 리스크 없음
